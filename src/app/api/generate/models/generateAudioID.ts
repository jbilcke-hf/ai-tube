export async function generateAudioID({
  prompt,
  // negativePrompt,
  seed,
}: {
  prompt: string
  // negativePrompt?: string
  seed?: number
}) {
  // TODO: call Parler-TTS
  return "TODO JULIAN"
}